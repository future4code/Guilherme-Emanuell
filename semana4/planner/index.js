function adicionarProduto() {
    const tarefa = document.getElementById("descTarefa").value
    
    /* tarefa.value = "" */
   /*  minhaLista.innerHTML += `<li>${meuInput.value}</li>` */
    
    
    console.log(tarefa)
}